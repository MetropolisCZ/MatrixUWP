<Page
    x:Class="MatrixUWP.StrankaJedenChat"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MatrixUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#44cccccc" Padding="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Image></Image>
            <TextBlock Text="Název skupiny" Grid.Column="1" VerticalAlignment="Center"></TextBlock>
            <Button Grid.Column="2" Background="Transparent">
                <SymbolIcon Symbol="More"></SymbolIcon>
            </Button>
        </Grid>

        <ListView x:Name="ListViewZpravyChaty" Grid.Row="1" HorizontalAlignment="Stretch" SelectionMode="None" IsItemClickEnabled="False" IsSwipeEnabled="False">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="Auto" Margin="4"> <!--HorizontalAlignment="{x:Bind MsgAlignment}"-->
                        <StackPanel MinHeight="75" Width="350" Padding="10, 0, 0, 10" CornerRadius="8">
                            <!--Background="{x:Bind BgColor}"-->
                            <TextBlock Padding="0, 10, 0, 0" FontSize="20" Foreground="{ThemeResource SystemControlForegroundAltHighBrush}"/>
                            <TextBlock Padding="0, 0, 0, 10" FontSize="15" Foreground="{ThemeResource SystemControlForegroundAltMediumHighBrush}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.Row="2" Background="#55cccccc" Padding="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBox BorderThickness="0" Padding="10"></TextBox>
            <Button Grid.Column="1" Background="{StaticResource SystemAccentColor}" VerticalAlignment="Stretch" VerticalContentAlignment="Center">
                <SymbolIcon Symbol="Send"></SymbolIcon>
            </Button>
        </Grid>

    </Grid>
</Page>
